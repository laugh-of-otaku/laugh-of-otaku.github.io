---
title: 数式の書き方
permalink: /math/
description: 研究ノートや日記に数式を書くための例。
---
研究ノート・日記・Aboutの本文に、LaTeX形式の数式を書けます。このサイトでは、文中の数式も独立した数式も `$$` で囲みます。

## 文中の数式

```markdown
エネルギーは $$E = mc^2$$ と書けます。
```

エネルギーは $$E = mc^2$$ と書けます。

## 分数と積分

数式を独立した行で表示するには、前後に空行を入れ、`$$` をそれぞれ別の行に置きます。

```markdown
$$
\int_0^1 x^2\,dx = \frac{1}{3}
$$
```

$$
\int_0^1 x^2\,dx = \frac{1}{3}
$$

## 行列

列は `&`、行は `\\` で区切ります。

```markdown
$$
A = \begin{pmatrix}
1 & 2 \\
3 & 4
\end{pmatrix}
$$
```

$$
A = \begin{pmatrix}
1 & 2 \\
3 & 4
\end{pmatrix}
$$

## よく使う書き方

| 内容 | 書き方 | 表示 |
| --- | --- | --- |
| 添字・累乗 | `$$x_i^2$$` | $$x_i^2$$ |
| 平方根 | `$$\sqrt{x}$$` | $$\sqrt{x}$$ |
| ギリシャ文字 | `$$\alpha + \beta$$` | $$\alpha + \beta$$ |
| 総和 | `$$\sum_{i=1}^{n} i$$` | $$\sum_{i=1}^{n} i$$ |

バッククォートで囲んだ部分は数式に変換されず、書き方の例として表示されます。実際に数式を表示したい箇所ではバッククォートを付けずに書いてください。

保存すると、ほかの本文と同じようにサイトへ反映されます。
